<p id="notice"><%= notice %></p>

<h1>Artículos</h1>

<table>
  <thead>
    <tr>
      <th>Título</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
        <td><%= link_to 'Mostrar', article %></td>
        <td><%= link_to 'Editar', edit_article_path(article) %></td>
        <td><%= link_to 'Eliminar', article, method: :delete, data: { confirm: 'Está seguro?' } %></td>
        </tr>
    <% end %>
  </tbody>
</table>

<br>
<!-- app/views/articles/index.html.erb -->
 <!-- <%= cl_image_tag("peh5gcku7z6dqlqe84tn", width: 400, height: 300, crop: :fill) %>-->
<br>

<%= link_to 'Nuevo Artículo', new_article_path %>
